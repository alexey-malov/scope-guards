# CMakeList.txt : CMake project for scope-guards, include source and define
# project specific logic here.
#

# Add source to this project's executable.
add_executable (scope-guards "main.cpp" "scope_guards.h")

if (CMAKE_VERSION VERSION_GREATER 3.30)
  set_property(TARGET scope-guards PROPERTY CXX_STANDARD 20)
endif()

if(MSVC)
  target_compile_options(scope-guards PRIVATE /W4 /WX)
else()
  target_compile_options(scope-guards PRIVATE -Wall -Wextra -Wpedantic -Werror)
endif()

# TODO: Add tests and install targets if needed.
